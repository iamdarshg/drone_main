# App module for main logic, path tracing, comms, frontend
# (Stub for now)

add_library(App STATIC
    main.c
    path_tracing.c
    comms.c
    frontend_cli.c
    mission.c
    init.c
)

# TODO: Implement main application logic
